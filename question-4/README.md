# **Question 4**

## getPointsForRow method
* This method takes in one int parameter and returns the point value of the targetted row.
* Loop through through the targetted row's column.
* Count the value of the current point value
* Check if the color is the same throughout the board, if different then the same color would be false.
* After looping through the whole column, if the same color is true, then the points would be doubled. Else, return the count.
* **The total point value of the column is returned**
